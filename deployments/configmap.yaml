# configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: contacts-config
data:
  POSTGRES_USER: "user"
  POSTGRES_PASSWORD: "password"
  POSTGRES_DB: "contacts"
  APPLICATION_PORT: "8080"
  DATABASE_URL: "postgres://user:password@contacts-db:5432/contacts?sslmode=disable"
  SECRET_KEY: "dhefefhdiqwjdijefwejif"
